[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "weather-console-app"
dependencies = [
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
    "terminaltables3>=3.0.0"
]